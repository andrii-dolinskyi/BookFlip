# BookFlip App

A web application for exchanging books between users.

## Features
- Search books by genre, language, page size, and more.
- User profiles with books for exchange.
- Direct messaging for arranging exchanges.

## Setup
1. Clone the repository.
2. Install dependencies:
   ```bash
   cd server
   npm install
   cd ../client
   npm install